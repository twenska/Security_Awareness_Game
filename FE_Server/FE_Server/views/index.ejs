<!DOCTYPE html>
<html>

<head>
    <title>Security Game - Registrieren</title>
    <meta charset="UTF-8">
    <link href="assets/styles/index.css" rel="stylesheet" type="text/css">
</head>

<body>
    <% include includes/nav.html %>

    <main>
        <div id='wrapper'>
            <div id='scores'>
                <table class="scores">
                    <tr>
                        <th>#</th>
                        <th>Benutzername</th>
                        <th>Punktzahl</th>
                        <th>Zeit</th>
                    </tr>
                    <% for(let i = 0; i < scores.length; i++) { %>
                    <tr>
                        <td><%= scores[i].id %></td>
                        <td><%= scores[i].user %></td>
                        <td><%= scores[i].score %></td>
                        <td><%= scores[i].time %></td>
                    </tr>
                    <% } %>
                </table>
            </div>
            <div id='right'>
                <div id='login'>
                    <h3>Bitte gib einen Benutzernamen ein</h3>
                    <form id="login_form" method="POST" action="https://game.twenska.de/fe/register">
                        <table>
                            <tr>
                                <td colspan="2"><span class='<%= msg.type %>'><%= msg.content %></span></td>
                            </tr>
                            <tr>
                                <td><label for="username">Benutzername</label></td>
                                <td><input type="text" name="username" placeholder="Benutzername"></td>
                            </tr>
                            <tr>
                                <td colspan="2"><input type="submit" value="   Spielen   "></td>
                            </tr>
                        </table>
                    </form>
                </div>
                <div id=''>
                    
                </div>
            </div>
        </div>
    </main>
    <% include includes/footer.html %>
</body>

</html>
